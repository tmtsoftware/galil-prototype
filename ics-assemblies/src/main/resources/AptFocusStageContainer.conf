# CSW container for the AptFocusStage assembly — runs it on its own JVM.
# Top-level key is `name` (ContainerInfo), matching the other containers.
name = "AptFocusStageContainer"

components: [
  {
    prefix = "aps.ICS.APT.FocusStage"
    componentType = assembly
    componentHandlerClassName = aps.ics.assembly.apt.AptFocusStageHandlers
    locationServiceUsage = RegisterAndTrackServices
    connections = [
      {
        # Galil controller 3 per SDD Figure 2-2. Must match the assembly
        # config axis `galilHcd`.
        prefix: aps.ICS.HCD.GalilMotion.3
        componentType: hcd
        connectionType: pekko
      }
    ]
  }
]
