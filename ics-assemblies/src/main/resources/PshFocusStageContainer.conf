# CSW container for the PshFocusStage assembly — runs it on its own JVM.
# Top-level key is `name` (ContainerInfo), matching the other containers.
name = "PshFocusStageContainer"

components: [
  {
    prefix = "aps.ICS.PSH.FocusStage"
    componentType = assembly
    componentHandlerClassName = aps.ics.assembly.psh.PshFocusStageHandlers
    locationServiceUsage = RegisterAndTrackServices
    connections = [
      {
        # Galil controller 1 per SDD Figure 2-2. Must match the assembly
        # config axis `galilHcd`.
        prefix: aps.ICS.HCD.GalilMotion.1
        componentType: hcd
        connectionType: pekko
      }
    ]
  }
]
