include "logging.conf"

pekko {
  loglevel = error
}

startManagement = false

csw-logging {

  component-log-levels {
    config-service-client = error
    location-service-lib = info
    aps.ICS.STIM.InsertionStage = debug
  }

  logLevel = trace
  slf4jLogLevel = trace
  pekkoLogLevel = trace

  appenders = ["csw.logging.client.appenders.FileAppender$", "csw.logging.client.appenders.StdOutAppender$"]

  appender-config {
    file {
      logPath = "/tmp/xxx"
    }
    stdout {
      color = false
      pretty = false
      oneLine = true
      fullHeaders = true
    }
  }
}
