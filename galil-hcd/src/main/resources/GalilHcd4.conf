# GalilMotion HCD Container — APS Galil Controller 4
# Registers CSW identity aps.ICS.HCD.GalilMotion.4. The HCD derives its
# per-instance config from this prefix (last segment "4"):
#   Config Service:  APS/ICS/HCD/GalilMotion/4.conf
#   offline fallback: GalilHcdConfig-APS-4.conf
# Do NOT pass -Dgalil.config.path here — that property is JVM-global and is only
# for single-HCD-per-JVM runs. Under host config each container is its own
# process, and prefix-derivation selects the right file.

name = "GalilHcdContainer4"

programs {
  resources = "programs"
  runtime   = "programs"
}

components: [
  {
    prefix = "aps.ICS.HCD.GalilMotion.4"
    componentType = hcd
    componentHandlerClassName = csw.proto.galil.hcd.GalilHcdHandlers
    locationServiceUsage = RegisterOnly
  }
]
